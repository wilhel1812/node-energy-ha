refresh:
  name: Refresh Node Energy
  description: Force immediate refresh/recompute for one or all Node Energy entries.
  fields:
    entry_id:
      name: Entry ID
      description: Optional config entry id to refresh.
      selector:
        text:
